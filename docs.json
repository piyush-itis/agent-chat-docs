{
  "$schema": "https://mintlify.com/docs.json",
  "theme": "mint",
  "name": "Galaxy Agent Chat",
  "logo": { "light": "/logo.svg", "dark": "/logo.svg" },
  "favicon": "/favicon.svg",
  "colors": {
    "primary": "#6D5EF7",
    "light": "#F4F4F5",
    "dark": "#0B0B0F"
  },
  "api": {
    "openapi": "openapi.yaml",
    "playground": {
      "display": "interactive"
    },
    "mdx": {
      "server": "https://agent-chat-backend.vercel.app",
      "auth": {
        "method": "bearer"
      }
    }
  },
  "navigation": {
    "groups": [
      {
        "group": "Guides",
        "pages": ["index", "authentication", "runs", "webhooks", "errors"]
      },
      {
        "group": "Tools",
        "pages": ["tools"]
      },
      {
        "group": "API playground",
        "openapi": "openapi.yaml",
        "pages": [
          "GET /v1/chats",
          "POST /v1/chats",
          "GET /v1/chats/{chatId}/messages",
          "POST /v1/chats/{chatId}/turns",
          "POST /v1/chat/completions",
          "POST /v1/tools/{toolName}",
          "GET /v1/runs/{runId}",
          "POST /v1/runs/{runId}/waitpoints/{token}/resume"
        ]
      }
    ]
  }
}
